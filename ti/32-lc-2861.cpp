class Solution {
public:
    int maxNumberOfAlloys(int n, int k, int budget, vector<vector<int>>& composition, vector<int>& stock, vector<int>& cost) {
        int ans=0;
        int mx=ranges::min(stock)+budget;
        for(auto& comp : composition) 
        {
            auto check = [&](long long num)->bool
            {
                long long money=0;
                for(int i=0;i<n;i++)
                {
                    if(stock[i]<num*comp[i])
                    {
                        money+=(num*comp[i]-stock[i])*cost[i];
                        if(money>budget)
                        {
                            return false;
                        }
                    }
                }
                return true;
            };

            int left=ans;
            int right=mx+1;
            while(left+1<right)
            {
                int mid=(right-left)/2+left;
                (check(mid)?left:right)=mid;
            }
            ans=left;
        }  
        return ans;
    }
};
/*
上面这段代码中的这一行
        int mx = ranges::min(stock) + budget;
是什么意思？

`int mx = ranges::min(stock) + budget;



` 这一行的作用是**为二分查找计算一个紧凑且安全的上界**——它表示在最优假设下（每种合金只需 1 份某金属且该金属单价为 1）理论上能制造的最大合金数，真实答案一定不超过这个值，因此可以用它作为二分的右端点。【turn0search0】【turn0search3】

## 拆解两个部分

- `ranges::min(stock)`：C++20 引入的范围库函数，返回 `stock` 数组中的最小库存量。这里取**最小**而非最大，因为合金生产是"短板效应"——每台机器对每种金属的需求量 `composition[i][j] >= 1`，所以无论用哪台机器，**每生产 1 份合金都至少消耗 1 份每一类金属**。最终能造多少份，被"最稀缺的那一类金属"卡住。
- `+ budget`：把全部预算都拿来购买金属的极端情况。由于 `cost[i] >= 1`，预算最多能换回 `budget` 份金属，因此任何一类金属 j 的可用总量上限就是 `stock[j] + budget`。【turn0search4】【turn0search5】

## 推导逻辑

题目约束保证 `composition[i][j] >= 1` 且 `cost[i] >= 1`，于是对任意金属 j：

- 每份合金至少消耗 1 份金属 j；
- 金属 j 的可用量最多是 `stock[j] + budget`（即便 `cost[j] = 1` 时把预算全花在它身上）。

把所有金属都考虑进来，合金数 ≤ min over j of (stock[j] + budget) = min(stock) + budget。这就得到了一个**保证不漏真解、又比朴素上界 `2×10^8` 更紧**的右端点。【turn0search0】

```mermaid
flowchart LR
    A["每份合金至少消耗<br/>1 份各类金属<br/>(composition ≥ 1)"] --> B["瓶颈金属 = 库存最少的那类<br/>min(stock)"]
    C["cost ≥ 1<br/>预算最多买 budget 份金属"] --> D["瓶颈金属可用总量<br/>≤ min(stock) + budget"]
    B --> D
    D --> E["合金数上界 mx<br/>= min(stock) + budget"]
    E --> F["作为二分右端点 right = mx + 1"]
```

## 一个具体例子

以示例 2 为例：`stock = [0,0,100]`，`budget = 15`。

- `min(stock) = 0`（第 1、2 类金属库存为 0）；
- `mx = 0 + 15 = 15`。

真实答案是 5，远小于 15，但 15 作为一个"保证不漏"的上界完全够用。如果用朴素上界 `2×10^8`，二分次数会从 `log₂(15) ≈ 4` 次膨胀到 `log₂(2×10^8) ≈ 28` 次，在 `k=100` 台机器的外层循环下差距明显。【turn0search3】

## 为什么只需"宽松上界"即可

二分答案类题目的核心是单调性：若能造 `x` 份则必能造 `x-1` 份，反之若不能造 `x` 份则必不能造 `x+1` 份。上界只需要**确保覆盖真实答案**，`check(mid)` 函数会在 `mid` 超过实际能力时返回 `false` 自动收缩右端点，因此 `mx` 不必精确，只要"够大且不溢出"即可。`min(stock) + budget` 在 `stock[i], budget ≤ 10^8` 下最大约 `2×10^8`，用 `int` 存放不会溢出，而 `check` 内部用 `long long` 计算避免 `comp[i] * num` 中途溢出。【turn0search7】【turn0search9】

简言之，这一行用一个"最优假设下的理论极限"收紧了二分区间，既保证正确性，又把时间复杂度从 `O(k·n·log(2×10^8))` 降到 `O(k·n·log(min(stock)+budget))`。



这里加budget的意思是，考虑的情况是：只买其中一份金属的情况，而且这个金属的价格为1？

你的理解抓住了关键的一环，但不完全准确。`+budget` 对应的并非"只买一种金属且该金属单价为 1"这一种具体情形，而是基于两个**最宽松假设**叠加出的理论上界：① 每台机器对每种金属的需求量 `composition[i][j] = 1`；② 每种金属的单价 `cost[i] = 1`。在这两个假设同时成立时，把全部预算花在瓶颈金属上才能凑出 `min(stock) + budget` 这么多份合金。【turn0search2】【turn0search4】

## 完整推导逻辑

真正的推导是"对每一类金属单独卡上限，再取最紧的那个"：

对任意金属 j，由于题目保证 `composition[i][j] >= 1` 且 `cost[i] >= 1`：

- 每份合金至少消耗 1 份金属 j，所以合金数 ≤ 金属 j 的可用总量；
- 金属 j 的可用总量 = 库存 `stock[j]` + 购入量 ≤ `stock[j] + budget`（因为单价 ≥ 1，预算最多买 `budget` 份）。

把这条不等式对所有 j 都成立，取最紧的那个就是 `min(stock) + budget`。【turn0search4】【turn0search13】

```mermaid
flowchart LR
    A["对任意金属 j"] --> B["每份合金消耗 ≥ 1 份 j<br/>(composition ≥ 1)"]
    A --> C["单价 ≥ 1<br/>预算最多买 budget 份 j<br/>(cost ≥ 1)"]
    B --> D["合金数 ≤ stock[j] + 购入量"]
    C --> E["购入量 ≤ budget"]
    D --> E
    E --> F["合金数 ≤ stock[j] + budget<br/>对每个 j 都成立"]
    F --> G["取最紧约束<br/>合金数 ≤ min(stock) + budget"]
    G --> H["作为二分右端点<br/>right = mx + 1"]
```

## 与你理解的差异

你的表述"只买其中一份金属，而且这个金属的价格为 1"只覆盖了"单价 = 1"这半个假设，但漏掉了两个要点：

- **还要假设 `composition = 1`**：若某台机器对瓶颈金属的需求是 2 份/合金，那么 `stock[j]` 份库存只能造 `stock[j]/2` 份，预算买的 `budget` 份也只能再造 `budget/2` 份，上界会缩到 `(stock[j] + budget) / 2`。只有当需求量也是 1 时，才能取到 `stock[j] + budget` 这个最大值。【turn0search2】
- **`min(stock)` 是对"所有金属"取最小，不是只盯某一种金属**：因为每份合金必须同时消耗每一类金属，瓶颈由库存最少的那类决定。即使你只买这一类金属，其他类别若库存更低照样卡住总量。

## 一个数值对照

以示例 2 为例：`stock = [0, 0, 100]`，`budget = 15`，`cost = [1, 2, 3]`。

- `min(stock) = 0`（第 1、2 类库存均为 0），所以 `mx = 0 + 15 = 15`。
- 真实答案是 5（用第 2 台机器 `composition = [1,1,10]`），因为第 3 类金属每份合金要 10 份、单价 3，预算根本撑不到 15 份。
- 但 `15` 作为上界依然安全：它是在"假设第 2 台机器对第 3 类金属需求降到 1、且第 3 类单价降到 1"这种最优假想下才可能触及的极限，真实答案绝不会超过它。【turn0search1】

## 小结

`+budget` 本质上是"在 `cost >= 1` 约束下，预算能补充的金属份数上限"，再配合 `composition >= 1` 得到每份合金对每类金属的消耗下限，二者相加构成"最优假想场景下"的合金数天花板。它不是某一种具体的购买策略，而是一个**保证不漏真解的宽松上界**——二分的 `check` 函数会自动把超出实际能力的 `mid` 判为 `false` 并收缩右端点，因此上界只需"够大且不溢出"即可，无需精确。【turn0search7】


以示例 2 为例：stock = [0, 0, 100]，budget = 15，cost = [1, 2, 3]。

min(stock) = 0（第 1、2 类库存均为 0），所以 mx = 0 + 15 = 15。
这里
mx等于15意思是最理想的情况下，最多只能生成15份吗？

是的，你的理解完全正确。`mx = 15` 指的就是在**理论上的最理想情况**下，最多只能生成 15 份合金。

## “最理想情况”的两个极端假设

这个 15 是基于两个极度乐观的假设叠加出来的：

1. **假设所有金属的单价都为 1**（实际 `cost = [1, 2, 3]`）
2. **假设每台机器对每种金属的需求量都为 1**（实际示例 2 中第 2 台机器 `[1,1,10]`）

在这两个假设同时成立时，对每一类金属计算“库存 + 预算可购入量”：

| 金属类型 | 库存 stock | 预算可买（假设 cost=1） | 可用总量 | 可支撑合金数（假设 composition=1） |
|:---:|:---:|:---:|:---:|:---:|
| 金属 1 | 0 | 15 | 15 | **15** |
| 金属 2 | 0 | 15 | 15 | **15** |
| 金属 3 | 100 | 15 | 115 | 115 |

瓶颈由库存最少的金属决定，即 `min(stock) + budget = 0 + 15 = 15`。

## 但实际约束会更紧

现实中，这两个假设都不成立，所以真正的上限会比 15 低得多。我们可以对每类金属算一个**真实的单金属约束**：

| 金属类型 | 库存 | 实际单价 | 预算可买 | 可用总量 | 可支撑合金数 |
|:---:|:---:|:---:|:---:|:---:|:---:|
| 金属 1 | 0 | 1 | 15 / 1 = 15 | 15 | 15 |
| 金属 2 | 0 | 2 | 15 / 2 = 7.5 | 7.5 | **7** |
| 金属 3 | 100 | 3 | 15 / 3 = 5 | 105 | 105 |

三者取最小值，真实的理论上界其实是 **7**（金属 2 的限制）。

再加上示例 2 中第 2 台机器对金属 3 的需求是 10 份/合金，进一步把最终答案压到了 **5**。

## 为什么代码不直接用 7？

因为 `min(stock) + budget` 这个公式：
- **计算简单**：一次 `min` 操作 + 一次加法
- **通用且安全**：不需要知道具体的 `cost` 数组细节
- **足够紧致**：最大不超过 `10^8 + 10^8 = 2×10^8`，二分查找只需 ~28 次迭代

而更精确的 `min(stock[j] + budget / cost[j])` 虽然更紧，但需要额外遍历计算，且对本题的复杂度优化帮助不大（都是 `O(log 10^8)` 级别）。

## 小结

```mermaid
flowchart LR
    A["mx = 15<br/>理论理想上界"] --> B["假设1: cost = [1,1,1]"]
    A --> C["假设2: composition = [1,1,1]"]
    B --> D["金属2实际可买: 15/2=7.5 → 7"]
    C --> E["金属3实际需求: 10份/合金"]
    D --> F["真实单金属约束: 7"]
    E --> G["实际答案: 5"]
    F --> G
```

所以 `mx=15` 确实是“最理想情况下最多生成 15 份”，它是一个**保证不漏掉真实答案的宽松上限**。二分查找会在 `[0, 15]` 这个范围内，通过 `check` 函数自动收缩到精确的真实答案 5。


| 金属类型 | 库存 stock | 预算可买（假设 cost=1） | 可用总量 | 可支撑合金数（假设 composition=1） |
|:---:|:---:|:---:|:---:|:---:|
| 金属 1 | 0 | 15 | 15 | **15** |
| 金属 2 | 0 | 15 | 15 | **15** |
| 金属 3 | 100 | 15 | 115 | 115 |


这里
即使每个金属售价为1，我也不能每个都买15份啊，
如果每个都买15份的话，我要45元，对吗？

你的直觉非常敏锐，**完全正确！** 你抓住了这个推导中最核心的逻辑。

之前的表格确实容易引起误解。预算 `budget=15` 是**总额**，不能给每种金属都分配15元。如果每种都买15份，总共要花45元，早就超支了。

`mx = min(stock) + budget` 这个上界，实际上基于一个更极端、更“脱离现实”的假设，我们称之为 **“孤注一掷”** 模型：

### 推导 `mx` 的真正逻辑：把全部预算只花在一种金属上

为了找到一个“绝对不可能被超越”的理论天花板，我们假设了以下最宽松的条件：
1. **除了库存最少的那一种金属，其他所有金属的库存都无限大。**（这样你就完全不需要花钱买其他金属了）
2. **制造一份合金只需要1份这种金属。**
3. **这种金属的单价是1元。**

在这个极端假设下，你不需要为其他任何金属花钱，所以你可以把**全部15元预算**都用来买这一种库存最少的金属。

让我们用示例 2 的数据重新算一遍：
`stock = [0, 0, 100]`，`budget = 15`

| 金属类型 | 实际库存 | 极端假设下的库存 | 是否需要花钱买 | 预算分配 | 最终可用总量 | 可支撑合金数 |
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| **金属 1** | 0 | **0 (瓶颈)** | **是** | **15元全给它** | 0 + 15 = **15** | **15** |
| 金属 2 | 0 | 假设为∞ | 否 | 0元 | ∞ | ∞ |
| 金属 3 | 100 | 假设为∞ | 否 | 0元 | ∞ | ∞ |

在这个“孤注一掷”的假设下，瓶颈只在金属1上，总共能凑出15份，所以理论上界 `mx = 15`。

### 为什么用这么离谱的假设？

因为二分查找只需要一个**绝对安全的上界**，保证真实答案一定在这个范围之内。

- **真实情况**：你不仅要买金属1，还要买金属2，预算必须分摊，而且金属2价格是2元，金属3每份合金需要10份。这些现实约束会让实际产量大幅缩水（真实答案是5）。
- **理论天花板**：`mx` 故意忽略了所有其他约束，只盯着最短板，算出了一个极度乐观的数字。

只要 `mx` 比真实答案大，二分查找就能在 `[0, 15]` 这个区间内，通过 `check()` 函数一个个验证，最终把因预算共享和实际消耗带来的现实约束重新加回来，准确定位到真实的5。

所以，你的质疑是对的：现实中不能每种都买15份。`mx=15` 的意思是：**即使你把15元全砸在一种金属上，最多也就只能造15份**。这就足以作为一个安全的边界了。


上面这段代码中：
这一段是什么意思？
auto check = [&](long long num) -> bool {
                long long money = 0;
                for (int i = 0; i < n; i++) {
                    if (stock[i] < comp[i] * num) {
                        money += (comp[i] * num - stock[i]) * cost[i];
                        if (money > budget) {
                            return false;
                        }
                    }
                }
                return true;
            };

作者：灵茶山艾府
链接：https://leetcode.cn/problems/maximum-number-of-alloys/solutions/2446024/er-fen-da-an-fu-ti-dan-by-endlesscheng-3jdr/
来源：力扣（LeetCode）
著作权归作者所有。商业转载请联系作者获得授权，非商业转载请注明出处。
解释一下


## 这段代码的含义

这是一个 **lambda 表达式**（匿名函数），作用是：**判断用当前这台机器制造 `num` 份合金，花费是否在预算 `budget` 以内。**

逐行解释如下：

```cpp
auto check = [&](long long num) -> bool {
```
- 定义一个 lambda 函数，`[&]` 表示捕获外部所有变量（`stock`、`comp`、`cost`、`budget`、`n`）
- 参数 `num`：要制造的合金数量
- 返回 `bool`：`true` 表示预算够用，`false` 表示超预算

```cpp
    long long money = 0;
```
- 累计总花费，初始为 0

```cpp
    for (int i = 0; i < n; i++) {
```
- 遍历每一种金属（共 `n` 种）

```cpp
        if (stock[i] < comp[i] * num) {
```
- 制造 `num` 份合金，第 `i` 种金属**需要** `comp[i] * num` 份
- 如果**库存不够**（`stock[i] < 需要的量`），就需要花钱买

```cpp
            money += (comp[i] * num - stock[i]) * cost[i];
```
- **需要购买的数量** = `comp[i] * num - stock[i]`（需要的 − 已有的）
- **花费** = 购买数量 × 单价 `cost[i]`
- 累加到总花费 `money`

```cpp
            if (money > budget) {
                return false;  // 提前剪枝：已经超预算了，不用继续算
            }
```

```cpp
    }
    return true;  // 所有金属都检查完，没超预算
};
```

---

## 举个例子

以示例 1 为例：`stock = [0,0,0]`, `cost = [1,2,3]`, `comp = [1,1,1]`, `budget = 15`

调用 `check(2)`（制造 2 份合金）：

| 金属 i | 需要 `comp[i]*2` | 库存 `stock[i]` | 需购买 | 花费 |
|--------|-----------------|----------------|--------|------|
| 0 | 2 | 0 | 2 | 2×1 = 2 |
| 1 | 2 | 0 | 2 | 2×2 = 4 |
| 2 | 2 | 0 | 2 | 2×3 = 6 |

总花费 = 2+4+6 = **12 ≤ 15** → 返回 `true` ✅

调用 `check(3)`：总花费 = 3+6+9 = **18 > 15** → 返回 `false` ❌

---

## 整体思路

外层对每台机器做**二分查找**，用这个 `check` 函数判断"制造 `mid` 份合金是否可行"，从而找到该机器能制造的最大合金数，最后取所有机器中的最大值。

            int left = ans, right = mx + 1;
这里为什么把left设置为ans，right设置为mx+1？

## 解释 `left = ans` 和 `right = mx + 1`

### 先回顾：开区间二分写法

```cpp
int left = ans, right = mx + 1;
while (left + 1 < right) {   // 循环条件
    int mid = left + (right - left) / 2;
    (check(mid) ? left : right) = mid;
}
ans = left;  // 最终答案就是 left
```

这是**开区间 `(left, right)`** 的二分模板：
- 不变量：`check(left) == true`，`check(right) == false`
- 循环结束时 `left + 1 == right`，答案 = `left`（最大的满足 check 的值）

---

### 为什么 `left = ans`？（下界优化）

`ans` 是**之前所有机器中已经找到的最大合金数**。

最终答案是所有机器的**最大值**，所以对于当前机器：
- 如果它比之前的机器差 → 不会更新 `ans`，`ans` 保持不变 ✅
- 如果它比之前的机器好 → 二分会找到更大的值 ✅

把 `left` 设为 `ans` 而不是 `0`，是**缩小搜索范围**的优化：

```
搜索范围从 (0, mx+1) 缩小为 (ans, mx+1)
```

> 即使 `left = 0` 也完全正确，只是多搜了几轮。

---

### 为什么 `right = mx + 1`？（上界）

```cpp
int mx = ranges::min(stock) + budget;
```

这是一个**理论上界**，推理如下：

| 条件 | 推导 |
|------|------|
| `composition[i][j] >= 1` | 制造 1 份合金，每种金属**至少**消耗 1 份 |
| 库存最少的那种金属只有 `min(stock)` 份 | 最多免费用 `min(stock)` 份 |
| `cost[i] >= 1` | 每份金属**至少**花 1 块钱 |
| 预算为 `budget` | 最多额外买 `budget` 份 |

所以：**最多制造 `min(stock) + budget` 份合金**，不可能更多。

因为是**开区间**，`right` 本身不在搜索范围内，所以设为 `mx + 1`：

```
搜索范围：(ans, mx+1)  →  实际搜索 ans+1, ans+2, ..., mx
```

---

### 直观图示

```
答案一定在这个范围内：

  ans          真正答案         mx
   |              |             |
   v              v             v
---[==============?=============]---
   left                      right-1
   
   left = ans (已知下界)
   right = mx + 1 (上界+1，开区间不含)
```

二分就是在这个范围内不断缩小，最终 `left` 收敛到真正的最大合金数。

这段代码中：
我在最外层的for循环中
每次循环进行二分，
我怎么找到最佳答案的？


## 如何找到最佳答案？

核心思路：**遍历每台机器 → 二分求该机器最大产能 → 用 `ans` 维护全局最大值**

---

### 整体流程图

```
ans = 0

机器1: 二分 → 最多做 2 份 → ans = max(0, 2) = 2
机器2: 二分 → 最多做 5 份 → ans = max(2, 5) = 5
机器3: 二分 → 最多做 3 份 → ans = max(5, 3) = 5  (不更新)

最终返回 ans = 5
```

---

### 关键：`ans = left` 这一步怎么实现"取最大值"？

```cpp
int left = ans, right = mx + 1;  // ← left 从当前 ans 开始
while (left + 1 < right) {
    int mid = left + (right - left) / 2;
    (check(mid) ? left : right) = mid;
}
ans = left;  // ← 更新 ans
```

分两种情况：

#### 情况 1：当前机器**更强**（能做 7 份，ans = 5）

```
left = 5,  right = mx+1
二分过程：check(6)=✓, check(7)=✓, check(8)=✗ ...
最终：left = 7
ans = 7  ✅ 更新了！
```

#### 情况 2：当前机器**更弱**（最多做 3 份，ans = 5）

```
left = 5,  right = mx+1
check(5) = ✗ → right 不断缩小
最终：right 缩到 6，left+1 == right，循环结束
left 还是 5（从未增大过）
ans = 5  ✅ 没变，保持之前的最优
```

> `left` 在二分中**只会增大，不会减小**，所以 `ans` 永远不会被错误地变小。

---

### 为什么这样写是对的？

| 设计 | 作用 |
|------|------|
| `left = ans` | 搜索下界 = 当前已知最优，**只关心能否超越它** |
| `ans = left` | 如果超越了就更新，没超越就保持不变 |
| 外层 `for` 循环 | 保证每台机器都被考虑 |

本质上等价于：

```cpp
// 等价写法（更直观但稍慢）
for (auto& comp : composition) {
    int left = 0, right = mx + 1;  // 从0开始搜
    while (left + 1 < right) {
        int mid = left + (right - left) / 2;
        (check(mid) ? left : right) = mid;
    }
    ans = max(ans, left);  // 显式取 max
}
```

灵茶山艾府的写法把 `max` 操作**隐含**在了 `left = ans` 的初始化中——既然 `left` 只增不减，`ans = left` 自然就完成了取最大值。

---

### 一句话总结

> 外层 for 保证**每台机器都试一遍**，二分找出每台机器的极限产能，`ans` 始终记录**所有机器中的最大值**，最终返回的就是答案。


意思是，我每次循环，都是从上次的最优结果出发，如果这次更好就替换？


对，你的理解完全正确。

```
ans = 0

第1台机器：从 0 出发二分 → 找到 2 → 比 0 好 → ans = 2
第2台机器：从 2 出发二分 → 找到 5 → 比 2 好 → ans = 5
第3台机器：从 5 出发二分 → 找不到比 5 大的 → ans 还是 5
```

就是**打擂台**：

- `ans` 是当前的"擂主"
- 每台机器上来挑战（二分搜索）
- 打得过就替换，打不过擂主不变

`left = ans` 的意思就是：**你至少得比擂主强，我才继续搜**，否则直接跳过，省时间。



*/