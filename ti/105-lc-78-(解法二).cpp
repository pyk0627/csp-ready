class Solution {
    vector<int> nums;
    vector<vector<int>> ans;
    vector<int> temp;
    void dfs(int depth)
    {
        ans.push_back(temp);
        if(depth==nums.size())
        {

            return;
        }
        for(int j=depth;j<nums.size();j++)
        {
            temp.push_back(nums[j]);
            dfs(j+1);
            temp.pop_back();
        }
    }
public:
    vector<vector<int>> subsets(vector<int>& nums) {
        this->nums=nums;
        dfs(0);
        return ans;
    }
};
/*
【视频】回溯不会写？套路在此！（Python/Java/C++/Go/JS/Rust）


灵茶山艾府
2022 感恩勋章
153513
2023.01.13
发布于 浙江
递归
回溯
C++
Go

5+
视频讲解
请看【基础算法精讲 14】，制作不易，欢迎点赞关注~

视频介绍了两种基本回溯思想：选或不选、枚举选哪个。

方法一：输入的视角（逗号选或不选）
假设每对相邻字符之间有个逗号，那么就看每个逗号是选还是不选。

也可以理解成：是否要在 i 和 i+1 处分割，把 s[i] 作为当前子串的最后一个字符，把 s[i+1] 作为下一个子串的第一个字符。

注意 s[n−1] 一定是最后一个字符，所以在 i=n−1 的时候一定要分割。

答疑
问：代码遇到回文串就加入 path，如何保证这种分割方案一定合法？

答：如果最后一个字符串不是回文串，我们不会递归到 i=n 的边界，不会把不合法的分割加入答案。

class Solution {
    bool is_palindrome(const string& s, int left, int right) {
        while (left < right) {
            if (s[left++] != s[right--]) {
                return false;
            }
        }
        return true;
    }

public:
    vector<vector<string>> partition(string s) {
        int n = s.size();
        vector<vector<string>> ans;
        vector<string> path;

        // 现在 s 未被分割的部分为 [start, n-1]
        // 当前位于下标 i，讨论是否在 i 和 i+1 之间切一刀
        auto dfs = [&](this auto&& dfs, int i, int start) {
            if (i == n) { // s 分割完毕
                ans.emplace_back(path);
                return;
            }

            // 不分割
            if (i < n - 1) { // i=n-1 时必须分割（这是最后一段），i<n-1 时才可以不分割
                dfs(i + 1, start);
            }

            // 分割，那么得到子串 [start, i]
            if (is_palindrome(s, start, i)) { // 判断子串 [start, i] 是不是回文串
                path.emplace_back(s.substr(start, i - start + 1));
                // 现在 s 未被分割的部分为 [i+1, n-1]
                dfs(i + 1, i + 1);
                path.pop_back(); // 恢复现场
            }
        };

        dfs(0, 0);
        return ans;
    }
};
复杂度分析
时间复杂度：O(n2 
n
 )，其中 n 为 s 的长度。每次都是选或不选，递归次数为一个满二叉树的节点个数，那么一共会递归 O(2 
n
 ) 次（等比数列和），再算上判断回文和加入答案时需要 O(n) 的时间，所以时间复杂度为 O(n2 
n
 )。
空间复杂度：O(n)。返回值的空间不计。
方法二：答案的视角（枚举子串结束位置）
class Solution {
    bool isPalindrome(const string& s, int left, int right) {
        while (left < right) {
            if (s[left++] != s[right--]) {
                return false;
            }
        }
        return true;
    }

public:
    vector<vector<string>> partition(string s) {
        int n = s.size();
        vector<vector<string>> ans;
        vector<string> path;

        // 现在 s 未被分割的部分为 [i, n-1]
        // 枚举下一刀切在哪
        auto dfs = [&](this auto&& dfs, int i) {
            if (i == n) { // s 分割完毕
                ans.emplace_back(path);
                return;
            }
            for (int j = i; j < n; j++) { // 枚举子串的结束位置
                if (isPalindrome(s, i, j)) { // 判断 [i, j] 是不是回文串
                    path.push_back(s.substr(i, j - i + 1)); // 分割！
                    // 现在 s 未被分割的部分为 [j+1, n-1]
                    dfs(j + 1);
                    path.pop_back(); // 恢复现场
                }
            }
        };

        dfs(0);
        return ans;
    }
};
复杂度分析
时间复杂度：O(n2 
n
 )，其中 n 为 s 的长度。答案的长度至多为逗号子集的个数，即 O(2 
n
 )，因此会递归 O(2 
n
 ) 次，再算上判断回文和加入答案时需要 O(n) 的时间，所以时间复杂度为 O(n2 
n
 )。
空间复杂度：O(n)。返回值的空间不计入

这里输入的视角和答案的视角有什么区别？给我讲懂


要彻底搞懂“输入视角”和“答案视角”的区别，我们可以用一句话来概括它们的核心差异：
* **输入视角**盯着 **“缝隙”** 看（做二选一：切还是不切）。
* **答案视角**盯着 **“块”** 看（做多选题：下一块有多长？）。

为了让你“秒懂”，我们先打个生动的比方，然后再拆解代码和递归树。

---

### 一、 形象的比喻：切一根长为 n 的香肠

假设你手里有一根长度为 $n$ 的香肠，你要把它切成若干段。

#### 1. 输入视角（选或不选）—— “走格子，遇缝做决定”
香肠上画了 $n-1$ 条虚线（刻度）。你沿着香肠从左往右走，每遇到一条虚线，你脑子里做一个**二选一**的决定：
* **不切**：跳过这条缝，继续往下走。
* **切**：在这里下刀！
**特点**：你是一步一步顺着香肠的下标在走，每次面临的都是 `Yes / No` 的分支。

#### 2. 答案视角（枚举选哪个）—— “拿刀块，直接定长度”
你站在香肠开头，直接问自己：“我切下来的**第一块**要多长？”
* 切 1 厘米？（然后对剩下的香肠重复这个过程）
* 切 2 厘米？（然后对剩下的香肠重复这个过程）
* 还是直接全拿走？
**特点**：你是在直接挑选“下一个答案（下一块香肠）”，每次面临的是一个 `for` 循环的多个选项。

---

### 二、 代码层面的拆解

以字符串 `"aab"`（长度 $n=3$）为例，看看两种视角在代码里的映射：

#### 🔪 方法一：输入视角（顺着下标走）
```cpp
auto dfs = [&](this auto&& dfs, int i, int start) {
    // i 代表当前考察的“缝隙”（字符 i 和 i+1 之间）
    // start 代表当前正在攒的这一块的“起点”
    
    // 1. 不切（跳过这个缝隙，继续攒，起点 start 不变）
    if (i < n - 1) { 
        dfs(i + 1, start); 
    }
    
    // 2. 切（在缝隙 i 这里下刀，判断 start 到 i 是不是回文）
    if (is_palindrome(s, start, i)) {
        path.emplace_back(s.substr(start, i - start + 1));
        dfs(i + 1, i + 1); // 切完后，下一块的起点变成了 i+1
        path.pop_back();
    }
};
```
* **决策对象**：缝隙 `i`。
* **参数冗余**：因为你不一定在 `i` 处切，所以你必须用额外的一个变量 `start` 来记住“我这一块是从哪里开始攒的”。

#### 🔪 方法二：答案视角（直接定长度）
```cpp
auto dfs = [&](this auto&& dfs, int i) {
    // i 代表剩下这部分香肠的“起点”（也就是上一刀的刀口）
    
    // 用 for 循环直接枚举“下一块”在哪里结束（也就是下一刀切在哪）
    for (int j = i; j < n; j++) { 
        if (isPalindrome(s, i, j)) { // 判断 [i, j] 这一块是不是回文
            path.push_back(s.substr(i, j - i + 1));
            dfs(j + 1); // 这一块切完了，下一块直接从 j+1 开始！
            path.pop_back();
        }
    }
};
```
* **决策对象**：下一刀的结束位置 `j`。
* **参数极简**：不需要 `start` 变量，因为 `i` 永远就是当前块的起点，`j` 就是终点，直接一步到位。

---

### 三、 递归树的形状对比

这是理解两者差异的最关键之处，它们生成的**递归树形状完全不同**，但最终找到的**答案集合是完全一样的**。

#### 🌳 输入视角的递归树（二叉树）
每个节点最多只有 **2 个分支**（左孩子=不切，右孩子=切）。
以 `"aab"` 为例（缝隙 0 在第一个 a 后，缝隙 1 在第二个 a 后）：
```text
             起点
           /      \
      切缝隙0    不切缝隙0
       /   \        /   \
    切缝隙1 不切   切缝隙1 不切
   (a,a,b) (a,ab) (aa,b) (aab)
```
* **缺点**：这棵树里包含了很多“不切”的无用分支（比如一直不切，直到最后才切），会产生很多多余的递归调用。

#### 🌳 答案视角的递归树（多叉树）
每个节点有 **多个分支**（取决于当前位置到末尾还有多少个字符）。
以 `"aab"` 为例：
```text
                 起点 (0)
               /   |    \
    第一块选"a"  选"aa"  选"aab"(非法)
      /    \       |
第二块选"a" 选"ab" 第二块选"b"
   |       (非法)   |
第三块选"b"       ["aa", "b"]
["a","a","b"]
```
* **优点**：这棵树**没有“不切”这种废话分支**。每一个分支都代表“我切实打实地切下了一块”，逻辑更直接，通常剪枝也更容易，是解决此类问题**更推荐**的写法。

---

### 四、 总结：做题时该用哪个？

灵茶山艾府总结的这两种视角，不仅适用于回文分割，而是**回溯算法的两大通用套路**：

1. **输入视角（选或不选）适合：**
   * **0-1背包问题**：每个物品“选”或“不选”。
   * **子集问题**：数组里的每个数字“要”或“不要”。
   * 当你觉得“枚举选哪个”逻辑太绕，不如直接顺着数组下标无脑往下走（写两个 DFS 分支）的时候。

2. **答案视角（枚举选哪个）适合：**
   * **组合/排列问题**：下一个位置我该填哪个数字？（`for` 循环遍历候选）。
   * **单词拆分 / 回文分割 / N皇后**：下一个“块”/“句子”/“皇后”该放在哪？
   * **大部分切割类、分段类问题**。这种方法通常代码更短、状态更少、执行效率也略高（因为没有那么多“跳过”的无效递归）。

**一句话带走：**
**输入视角**是顺藤摸瓜（沿着数组下标，走一步做一步决定）；
**答案视角**是直接摘瓜（用 `for` 循环，直接凑出下一个满足条件的答案组件）。


class Solution {
    vector<int> nums;
    vector<vector<int>> ans;
    vector<int> temp;
    void dfs(int depth)
    {
        ans.push_back(temp);
        if(depth==nums.size())
        {

            return;
        }
        for(int j=depth;j<nums.size();j++)
        {
            temp.push_back(nums[j]);
            dfs(j+1);
            temp.pop_back();
        }
    }
public:
    vector<vector<int>> subsets(vector<int>& nums) {
        this->nums=nums;
        dfs(0);
        return ans;
    }
};
为什么上面的代码是以答案为视角的？
这个循环是什么意思：
        {
            temp.push_back(nums[j]);
            dfs(j+1);
            temp.pop_back();
        }



        要理解这段代码为什么是“答案视角”，我们首先要抓住区分这两种视角的**核心黄金法则**：

* **输入视角**的核心动作是：**“做判断题”**。每次递归只盯着输入数组里的**某一个固定元素**，问自己：“选它，还是不选它？”（代码里通常有两个分支，没有 `for` 循环）。
* **答案视角**的核心动作是：**“做多选题”**。每次递归盯着的是 **“我当前正在构造的答案”**，问自己：“为了完善这个答案，我的下一个元素该从剩下的候选里挑谁？”（代码里通常有一个 `for` 循环来枚举选项）。

下面为你详细拆解这两个问题。

### 一、 为什么这段代码是“答案视角”？

我们可以把“求子集”想象成 **“组建一支球队”**。
* **输入视角（顺着人挑）**：你手里有一份名单（0号张三，1号李四...）。你顺着名单往下看，看到张三：“要他，不要他？”；看到李四：“要他，不要他？”... 你的注意力在**名单（输入）** 上。
* **答案视角（顺着位置挑）**：你站在球场边，看着手里的空名单（子集）。你想：“我现在球队里有0个人，我的**第1个球员**该选谁？选张三？选李四？”；选完张三后，你想：“我的**第2个球员**该选谁？”... 你的注意力在**球队名单（答案）** 上。

你提供的代码里，有一个 `for` 循环：
```cpp
for(int j=depth;j<nums.size();j++)
```
这个 `for` 循环完美体现了“做多选题”的特征。它不是在处理固定的某个输入元素，而是在说：**“当前这一层，我要为子集挑选下一个元素，我可以在 `[depth, n-1]` 这些候选人里任意挑一个 `j`。”**

每次调用 `dfs`，都代表**你往答案里实实在在地塞进了一个元素**（即答案的长度增加了）。这就是典型的“关注答案构造过程”的答案视角。

*(注：你的代码中变量名 `depth` 叫“深度”稍微有点误导人，它其实代表的是 **“下一次挑选的起始下标”**，如果叫 `start_index` 会更直白。)*

---

### 二、 这个循环是什么意思？

我们把这三行代码拆开，用 **“去餐厅点菜”** 的比喻来解释：

假设 `nums` 是餐厅的菜单，`temp` 是你手里的点菜单，`ans` 是最终你要交给后厨的所有可能的点单方案。

```cpp
for(int j=depth;j<nums.size();j++) // 从当前允许的点菜位置开始，看每一道菜
{
    // 1. 做决定：点这道菜
    temp.push_back(nums[j]); 
    
    // 2. 交给下一层：点完这道菜，看看剩下的菜还能怎么点
    dfs(j+1); 
    
    // 3. 撤销决定：后悔了，把这道菜划掉，尝试点下一道菜
    temp.pop_back(); 
}
```

#### 逐行深度剖析：

**1. `temp.push_back(nums[j]);` （做决定）**
* **含义**：我决定把 `nums[j]` 作为当前子集的“下一个元素”。
* **点菜比喻**：我决定把菜单上的第 `j` 道菜写进我的点菜单里。

**2. `dfs(j+1);` （进入下一层）**
* **含义**：因为子集通常不考虑顺序（`[1,2]` 和 `[2,1]` 是同一个子集），为了避免重复，我选了 `j` 之后，下一个元素**只能从 `j` 后面的人里挑**。所以把起始下标设为 `j+1`，交给下一层递归去挑下一个元素。
* **点菜比喻**：我已经点了第 `j` 道菜，接下来我看看剩下的菜还能怎么组合。

**3. `temp.pop_back();` （恢复现场/撤销决定）**
* **含义**：这是回溯算法的灵魂！当以 `nums[j]` 为首的所有子集都找完了（递归结束了），我必须把 `nums[j]` 从当前子集里拿出去，**腾出位置**，好让 `for` 循环的下一次迭代（也就是 `nums[j+1]`）能加进来。
* **点菜比喻**：以第 `j` 道菜为主打菜的所有套餐都配完了。现在我要把点菜单上的第 `j` 道菜擦掉，尝试把第 `j+1` 道菜写上去，看看能配出什么新套餐。

---

### 三、 对比：如果是“输入视角”，代码长什么样？

为了让你彻底死心，我们看看同样求子集，**“输入视角（选或不选）”** 的代码是怎样的。你会发现它**没有 `for` 循环**，只有两个平行的 `dfs`：

```cpp
void dfs(int i) { // i 代表当前正在考察的输入数组下标
    // 1. 走到输入数组末尾，说明所有元素都考察过了，收集答案
    if(i == nums.size()) {
        ans.push_back(temp);
        return;
    }

    // 分支 1：不选 nums[i] （直接考察下一个）
    dfs(i + 1);

    // 分支 2：选 nums[i]
    temp.push_back(nums[i]);
    dfs(i + 1); // 选完也要考察下一个
    temp.pop_back(); // 恢复现场
}
```

**对比总结：**
* **你的代码（答案视角）**：用 `for` 循环枚举下一个元素选谁。**没有“不选”这个选项，只要进入下一层，必定是选了一个元素。**（多叉树）
* **输入视角代码**：用两个分支，明确写出“不选”和“选”。**沿着输入数组的下标 0, 1, 2... 一条道走到黑。**（二叉树）

所以，你的代码是标准且优雅的**答案视角**解法！


*/