class Solution {
public:
    int findMin(vector<int>& nums) {
        int left=-1;
        int right=nums.size()-1;
        while(left+1<right)
        {
            int mid=(left+right)/2;
            if(nums[mid]>nums[nums.size()-1])
            {
                left=mid;
            }else
            {
                right=mid;
            }
        }
        return nums[right];
    }
};
/*
为什么每次二分的时候，是和最后一个元素比较？

## 一句话先答

二分能用的前提是数组具有**二段性**（能被一个判断条件切成连续的两段，左段全 false、右段全 true，分界点就是答案）。

选 `nums[-1]` 当参照，是因为它**正好落在“含最小值的那一段”的右端点上**。这带来两个好处：

1. 以它为阈值，数组在**任何情况**（包括根本没旋转）下都是干净的二段性；
2. 我们要找的“蓝色段”永远非空，所以这个红蓝模板**不需要任何特判**。

而 `nums[0]` 落在**另一段**的端点上，用它做参照会在“未旋转”时让目标段变空，必须额外特判。

下面把这件事彻底拆开看。

---

## 1. 旋转数组的“两个台阶”结构

把升序数组旋转，相当于把一条上升折线切断、把后半截搬到前面，于是形成**左高右低两个台阶**，且左台阶的每个数都 > 右台阶的每个数（元素互不相同）：

```
原数组:        1 2 3 4 5 6 7
旋转 3 次:     5 6 7 │ 1 2 3 4
               ────────────────
               左段(大)  右段(小, 含最小值)
               ↑nums[0]        ↑nums[-1]
                       ↑最小值(右段第一个)
```

我们要找的最小值 = **右段的第一个元素** = 两个台阶的分界点。

---

## 2. 为什么和 `nums[-1]` 比就有二段性

看 `nums[-1]` 站在哪：它是**右段的最右端**（右段内部升序，所以它还是右段的最大值）。于是：

- **左段**的每个数都 `> nums[-1]`（左台阶整体比右台阶高）；
- **右段**的每个数都 `<= nums[-1]`（右段从最小值一路升到 `nums[-1]`）。

所以判断条件 `nums[mid] < nums[-1]` 把数组切成：

```
[  红: >= nums[-1]  |  蓝: < nums[-1]  ]
   左段(大)            右段去掉末尾
```

蓝色段的**第一个位置**就是最小值。这正好对应你截图里的模板：染蓝就 `right = mid`，循环结束 `right` 停在第一个蓝色，`return nums[right]` 即最小值。✅

---

## 3. 为什么不用 `nums[0]`（关键对比）

`nums[0]` 站在**左段的最左端**。乍一看 `nums[mid] < nums[0]` 也能切（左段 `>=`、右段 `<`），但它在**未旋转**时会塌掉：

| 情况 | 用 `nums[-1]` 作参照 | 用 `nums[0]` 作参照 |
|---|---|---|
| `[5,6,7,1,2,3,4]` 有旋转 | 蓝段=`[1,2,3]`，第一个=1 ✅ | 蓝段=`[1,2,3,4]`，第一个=1 ✅ |
| `[1,2,3,4,5]` **未旋转** | `nums[-1]=5`，所有数 `<=5`，蓝段=整数组，第一个=`nums[0]=1` ✅ | `nums[0]=1` 已是最小值，**没有任何数 `<1`，蓝段为空** ❌ |

未旋转时，用 `nums[0]` 的二分找不到分界点（`right` 会停在 `n-1`，返回最大值，错），**必须先特判** `if nums[0] < nums[-1]: return nums[0]`。

> 你可能会想：那把条件改成 `nums[mid] <= nums[0]` 不就行了？
> 不行。未旋转时只有 `nums[0]` 自己满足 `<=nums[0]`，它孤零零待在索引 0，和右边的“小段”不连续 —— **二段性被破坏了**，二分同样失效。

---

## 4. 本质：参照值要选在“你想定位的那一段”里

把上面归纳成一条规律，以后所有这类二分都通用：

> **你希望二分最终收敛到的那一段，必须非空；最稳妥的办法是让参照值本身就落在这一段里。**

- 本题要定位的是**含最小值的右段**。
- `nums[-1]` 是右段的右端点 → 它**永远在右段里** → “`< nums[-1]` 的那一段”至少包含最小值，**永远非空、永远连续** → 模板直接跑，零特判。
- `nums[0]` 是左段的左端点 → 它**不在右段里** → 当右段本身为空（未旋转）时，目标段就空了 → 必须特判。

这就是“每次都和最后一个元素比”的根本原因：**不是不能和第一个比，而是和最后一个比能把‘未旋转’这个讨厌的边界情况免费吸收掉，让二段性在所有输入上都成立。**

---

## 5. 用你的代码走一遍未旋转，体会“免费吸收”

`nums = [1,2,3,4,5]`，`nums[-1]=5`，初始 `left=-1, right=4`：

- mid=1, nums[1]=2 < 5 → 蓝 → right=1
- mid=0, nums[0]=1 < 5 → 蓝 → right=0
- 循环结束（left+1==right），返回 `nums[0]=1` ✅

全程没有任何 `if 特判`，蓝段一路把 `right` 推到 0，自动得到正确答案。换成 `nums[0]` 参照，这一步就做不到。

---

**记忆口诀**：旋转数组找最小值，参照选**右端点** `nums[-1]`，条件 `<`，找第一个染蓝的位置；因为右端点天然站在“答案所在的那一段”里，二段性永不破、目标段永不空。
*/