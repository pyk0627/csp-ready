class Solution {
public:
    int lengthOfLongestSubsequence(vector<int>& nums, int target) {
        vector<int> memo(target+1,INT_MIN);
        memo[0]=0;
        for(auto x:nums)
        {
            for(int c=target;c>=x;c--)
            {
                memo[c]=max(memo[c],memo[c-x]+1);
            }
        }
        return memo[target]>0?memo[target]:-1;
    }
};
/*
为什么把这个的INT_MIN换成-1，会出错？

这是一个非常深刻且经典的问题！把 `INT_MIN` 换成 `-1` 会导致错误的核心原因在于：**`-1` 在加上 `1` 之后会变成 `0`，这会让“无法凑出的非法状态”被程序误认为是“合法且长度为 0 的有效状态”，从而污染后续的计算。**

我们可以通过**状态转移的本质**和**一个具体的反例**来彻底弄懂这个问题。

### 1. 状态转移的本质：什么是“有效”，什么是“无效”？

在这道题中，`memo[c]` 的含义是：**凑出总和恰好为 `c` 的最长子序列的长度（即元素个数）**。
* **有效状态**：`c` 能够被凑出来。此时 `memo[c] >= 0`（比如凑出 5 需要 2 个元素，`memo[5] = 2`）。
* **无效状态**：`c` 根本**无法被凑出来**。此时 `memo[c]` 必须是一个**足够小的负数**，代表“此路不通”。

当程序执行状态转移时：
```cpp
memo[c] = max(memo[c], memo[c - x] + 1);
```
它的逻辑是：如果我想知道凑出 `c` 的最长长度，我可以看看**凑出 `c - x` 的最长长度**，然后**加上当前这 1 个元素 `x`**。

**致命问题就出在 `memo[c - x] + 1` 这一步：**
* 如果 `c - x` 是**无法凑出**的（无效状态），我们**绝对不能**在它的基础上 `+1`，因为“不存在的东西加上 1 依然是不存在”。
* 如果你把初始值设为 `-1`，当 `c - x` 无法凑出时，`memo[c - x]` 就是 `-1`。
* 此时 `memo[c - x] + 1` 就变成了 `-1 + 1 = 0`！
* 程序一看：`0` 是个非负数啊！它会把 `0` 当作一个**合法的有效状态**（认为凑出 `c` 需要 0 个元素），并把这个错误的结果存入 `memo[c]` 中。这就叫**状态污染**。

---

### 2. 举个反例：看看 `-1` 是怎么导致 Bug 的

假设 `nums = [2, 2]`，`target = 5`。
显然，2+2=4，我们**永远凑不出 5**，正确答案应该是 `-1`。

**如果初始化为 `-1`：**
初始数组：`memo = [0, -1, -1, -1, -1, -1]` （索引 0 到 5）

**处理第一个 `2`：**
* `c = 5`: `memo[5] = max(-1, memo[3] + 1) = max(-1, -1 + 1) = max(-1, 0) = 0`  👉 **错误发生！`memo[5]` 变成了 0。**
* `c = 4`: `memo[4] = max(-1, memo[2] + 1) = max(-1, -1 + 1) = 0`
* `c = 3`: `memo[3] = max(-1, memo[1] + 1) = max(-1, -1 + 1) = 0`
* `c = 2`: `memo[2] = max(-1, memo[0] + 1) = max(-1, 0 + 1) = 1` (正确)
第一轮结束后：`memo = [0, -1, 1, 0, 0, 0]`

**处理第二个 `2`：**
* `c = 5`: `memo[5] = max(memo[5], memo[3] + 1) = max(0, 0 + 1) = 1` 👉 **严重错误！程序认为凑出 5 需要 1 个元素！**
* ... 其他计算略 ...

**最终结果：**
程序返回 `memo[5]`，也就是 **`1`**。
但实际上 5 根本凑不出来，应该返回 `-1`。这就是 `-1` 导致的严重 Bug！

---

### 3. 为什么 `INT_MIN` 能完美解决？

`INT_MIN` 是 C++ 中 `int` 类型能表示的最小值（通常是 `-2147483648`）。

当我们使用 `INT_MIN` 作为无效状态时：
* 如果 `c - x` 无法凑出，`memo[c - x]` 就是 `INT_MIN`。
* 计算 `memo[c - x] + 1` 时，结果是 `INT_MIN + 1`（即 `-2147483647`）。
* 在 `max` 比较时：`max(有效值, -2147483647)`。
* 因为任何有效的长度（哪怕是 0）都**远远大于** `-2147483647`，所以 `max` 函数会**毫不犹豫地抛弃这个无效状态**，保留原本的有效值。

**`INT_MIN` 就像一个深不见底的黑名单，任何无效状态掉进去后，就算给它 `+1`、`+100`，它依然是个极小的负数，永远无法翻身变成合法状态去污染别人。**

---

### 💡 总结与实战建议

在动态规划中，初始化“无效状态”有一个黄金法则：
1. **求最大值（如本题的最长长度、最大价值）**：无效状态必须初始化为**极小的负数**（如 `INT_MIN` 或 `-1e9`），防止 `+1` 后变成合法状态。
2. **求最小值（如零钱兑换的最少硬币数）**：无效状态必须初始化为**极大的正数**（如 `INT_MAX / 2` 或 `1e9`），防止 `+1` 后溢出，且在 `min` 比较中被淘汰。

**实战小贴士：**
虽然 `INT_MIN` 在这道题里不会出错，但在某些复杂的 DP 中，如果无效状态被加了很大的数，`INT_MIN + 大数` 可能会导致整数溢出变成正数。因此，在工程实践和刷题中，大家更喜欢用一个**“足够小但不会溢出的常数”**，比如 **`-1e9`** (即 `-1000000000`)，或者 **`-100000`**，这样代码更健壮！

```cpp
// 更推荐的健壮写法
vector<int> memo(target + 1, -1e9); // 用 -1000000000 代替 INT_MIN
memo[0] = 0;
```
*/